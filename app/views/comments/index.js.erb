// エラーメッセージのリフレッシュのためにform部分を再描画
$("#comments_list").html("<%= j(render 'comments/index', { comments: @comment.topic.comments, topic: @comment.topic }) %>")
$("#form_area").html("<%= j(render 'comments/form', { comment: @comment, topic: @topic }) %>")
$(':text').val('')
