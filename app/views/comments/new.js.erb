$("#form_area").html("<%= j(render 'comments/form', { comment: @comment, topic: @topic }) %>")
$("#comments_area").html("<%= j(render 'comments/index', { comments: @comment.topic.comments, topic: @comment.topic }) %>")
$(':text').val('')
